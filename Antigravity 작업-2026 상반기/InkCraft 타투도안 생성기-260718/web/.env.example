# Firebase 웹앱 설정 — Firebase 콘솔 > 프로젝트 설정 > 내 앱 > SDK 설정에서 복사.
# 이 파일을 .env로 복사해서 값을 채우면 앱이 자동으로 "Firebase 모드"(로그인+코인)로 전환된다.
# 비워두면 "로컬 모드"(server/ 프록시 + IP 일일한도)로 동작한다.
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_APP_ID=
