rules_version = '2';

// Share images are uploaded server-side via the Admin SDK and made public
// per-file, so no client-side access is required.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
